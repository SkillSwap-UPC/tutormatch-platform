/**
 * Represents a query to retrieve all courses.
 */
export class GetAllCoursesQuery {
  /**
   * Constructor without parameters as this query doesn't require any filtering.
   */
  constructor() {}
}