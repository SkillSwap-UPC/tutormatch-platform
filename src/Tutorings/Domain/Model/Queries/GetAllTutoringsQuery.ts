/**
 * Represents a query to retrieve all tutoring sessions.
 */
export class GetAllTutoringsQuery {
  /**
   * Constructor without parameters as this query doesn't require any filtering.
   */
  constructor() {}
}